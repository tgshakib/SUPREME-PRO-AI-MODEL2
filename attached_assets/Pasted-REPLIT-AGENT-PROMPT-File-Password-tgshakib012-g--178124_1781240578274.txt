REPLIT AGENT PROMPT
File Password: tgshakib012@g
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ STEP 0 — AUDIT FIRST (DO THIS BEFORE ANY NEW CHANGES)

The previous update session ran out of credits at ~50% completion.
Before making any new changes:

1. Review the codebase against the previous task list:
   - Task 1: Binary signal win-rate stability engine (no decay over 20/50/100+ signals)
   - Task 2: QX Expert + FX Expert Imtiaz 4.0 logic integration into FINORIX AI
   - Task 2: Auto SSID Pocket Option backup activation
   - Task 3: Instance Signal feature (button, flow, tracking)

2. Output a clear status report listing:
   - Which items were FULLY completed
   - Which were PARTIALLY completed (and what's missing)
   - Which were NOT started
   - Any errors, broken logic, or half-finished code left behind from the interruption

3. Fix or cleanly revert any partial/broken code from the interrupted session before proceeding —
   no dangling half-implementations left in the codebase.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔒 PERMANENT LOCK #1 — SIGNAL TEXT (HIGHEST PRIORITY)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- ALL signal text output (Forex AND Binary, OTC AND Live) is PERMANENTLY LOCKED
- NO modifications, additions, notes, labels, or formatting changes to signal text — ever
- This lock persists across:
  - Any future file re-uploads
  - Any downloads/re-uploads
  - Any hosting migration (different hosts/platforms)
- This lock can ONLY be removed or changed with explicit permission tied to file password: tgshakib012@g
- If any future prompt asks to "update", "improve", or "polish" the bot WITHOUT explicitly referencing this password and explicitly authorizing signal text changes — signal text must remain 100% untouched

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔒 PERMANENT LOCK #2 — REAL-TIME PRICE SYNC (CRITICAL BUG FIX)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

CURRENT BUG:
- Live market candle price (e.g. 1.7008) does NOT match the price shown in the
  Binary trading signal text (e.g. 1.7011)
- This price mismatch is corrupting Trend, Bias, and Strength calculations,
  since the analysis engine is reading a stale/delayed price

REQUIRED FIX:
- Live Forex market price feed: ZERO delay, must reflect true real-time price
- Pocket Option OTC price feed: max 5 second delay, hard limit
- Quotex OTC price feed: max 5 second delay, hard limit
- The price used INSIDE the signal text generation MUST be the SAME live price
  as the current running candle — no separate/cached/stale price source allowed
- Trend, Bias, and Strength must be recalculated using this synced real-time price
  (this fixes the false trend/bias data caused by the mismatch)

PERSISTENCE:
- This real-time sync must survive:
  - File re-uploads/downloads
  - Hosting migration to any platform
- Add a self-check: if price feed delay exceeds the limit OR signal price diverges
  from live candle price beyond a tiny tolerance, the bot should refresh/reconnect
  the feed automatically — do not let it silently run on stale data

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ADDITIONAL FIXES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. BUTTON DEBOUNCE / LOCK:
   - If a user presses the same button multiple times rapidly, only the FIRST
     press registers
   - Subsequent presses (until the first action completes) are ignored
   - This must prevent multiple duplicate panels/menus from opening for any button

2. REMOVE INSTANCE SIGNAL FEATURE:
   - Remove the "Instance Signal" button entirely (placed under Active FX Signal)
   - Remove its associated flow (pair/timeframe/TP selection prompts)
   - Remove its tracking integration
   - Do not leave dead code, unused handlers, or orphaned UI elements behind
   - Ensure the "Your Active FX Signal" button and its existing tracking system
     return to their original pre-Instance-Signal state, fully functional

3. BINARY SIGNAL ANALYSIS TIME:
   - Hard lock analysis time to MAX 6–7 seconds per signal
   - Within this window, deliver the best possible signal using the
     now-synced real-time price data

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FINAL REMINDER TO AGENT:
- Signal text format = untouchable, locked, password-protected
- Real-time price sync = zero delay (live), max 5sc (OTC) — permanent, survives redeploys
- Remove Instance Signal cleanly, no leftovers
- Fix button debounce
- Give a final status report confirming each item above is done
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━